# simplecall

Perform basic syscalls with given arguments.

See [simplecall(1)](./simplecall.sc) for docs.

## Build and install

[scdoc(1)](https://git.sr.ht/~sircmpwn/scdoc) is needed to generate the man page.

```sh
git submodule update --recursive --init
make
make install
```
